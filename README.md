# Voice Calendar — AI 语音日历

> **"说了就算"** — 用一句话创建、管理你的全部日程。
> 语音是它的主要交互方式，AI 是它的理解引擎。

---

## 为什么做这个产品

人们每天口头约定大量日程，但传统日历 App 的输入路径是：

> 解锁 → 打开应用 → 点「+」→ 填标题 → 选日期 → 选时间 → 填地点 → 保存

至少 8 步操作。开车、做饭、走路时根本不可用。

Voice Calendar 用一句话替代八步操作：只需说"明天下午三点和小张聊项目"，系统自动提取时间、事件并创建日程。

### 市场空白

| 事实 | 说明 |
|------|------|
| 主流竞品不支持中文 | Fantastical、Motion、Morgen 的自然语言解析仅支持英语 |
| 平台助手能力有限 | Siri/小爱只能处理简单指令，无法理解"工作日每天早上九点站会" |
| 语音优先产品稀缺 | 目前仅有 0sec (iOS) 是纯 voice-first 日历，且仅支持英语 |
| 国内无对标产品 | 搜索"语音日历"无专注该品类的独立产品 |

---

## 目标用户

### 核心用户

**高效能职场人** — 日均 5-8 个会议，日程变动频繁。开车/走路时突然想到要加日程，打字操作繁琐。核心诉求：一句话完成日程录入。

**银发族** — 需要记药时间、社区活动、子女拜访。不会打字或打字很慢，日历 App 界面复杂。核心诉求：像说话一样自然地管理日程。

**学生 / 自由职业者** — 日程来源碎片化（口头约定、临时通知），手动整理耗时。核心诉求：语音快速捕获，智能理解"大后天""下周三四"等模糊表达。

### 次要用户

- **车载 / 运动场景用户** — 免提操作是安全刚需
- **家庭管理者** — 统筹全家日程（孩子课外班、老人就医）

---

## 产品定位与演进

Voice Calendar 定位为**语音优先的日历管理工具**，当前聚焦 L1 阶段。

```
L1 语音日历助手（当前版本）
  语音 → 结构化事件（CRUD），单轮 + 简单多轮交互
  │    智能解析引擎 + 确认卡片 + 语音播报闭环
  │
L2 时间管理 Agent（规划中）
  多轮对话 + 上下文记忆 + 冲突检测
  │    "把下午的会都往后推半小时"
  │    跨工具：日历 + 提醒 + 闹钟
  │
L3 个人效率 Agent（远期）
  学习用户习惯 → 主动建议
       "你最近三周都没运动，要不要安排一次？"
```

---

## 核心特性（当前已实现）

### 语音交互

- **按住说话** — 按住麦克风按钮说话，松开识别。短按可切换开关
- **多语言识别** — 支持中文（普通话）、英文、日语三种语音识别，跟随界面语言自动切换
- **实时中间结果** — 说话过程中实时显示识别中的文字
- **语音播报闭环** — 操作完成后语音播报结果（"已为您安排明天下午两点的会议"），形成"说话→确认→播报"完整闭环

> **技术说明：** 语音识别基于浏览器原生 Web Speech API，需要 Chrome/Edge 浏览器且设备有麦克风。Safari 部分支持，Firefox 不支持。

### AI 自然语言解析

- **LLM 驱动** — 接入 DeepSeek / OpenAI 等大语言模型，理解自然语言指令
- **结构化输出** — 将语音文字解析为 intent + params 结构化数据
- **支持 7 种意图** — 创建事件、删除事件、修改事件、查询事件、确认、取消、无法理解
- **中文相对时间** — 支持"明天""后天""下周三""工作日每天""这周"等常见中文时间表达
- **英文/日文指令** — 切换语言后可用对应语言说指令
- **歧义处理** — 当指令有歧义时（如"删掉明天的会"但明天有 3 个事件），列出候选让用户选择

### 事件管理

- **创建事件** — 语音指令解析后创建，包含标题、开始/结束时间、提醒
- **删除事件** — 支持模糊匹配，歧义时展示候选列表
- **修改事件** — 支持修改时间、标题等字段（多匹配时需选择）
- **查询事件** — 查询指定日期/范围的事件列表，语音播报结果
- **手动创建** — 点击日历空白区域弹出表单手动添加
- **事件详情** — 点击已有事件查看详情、编辑或删除

### 日历视图

- **月视图** — 默认视图，显示整月事件概览
- **周视图** — 按小时显示一周时间轴
- **日视图** — 按小时显示当天时间线
- **今日高亮** — 当前日期以主题色圆形标记

### 提醒

- **定时提醒** — 创建事件时设定提前 N 分钟提醒
- **浏览器通知** — 提醒触发时弹出系统通知（需用户授权）
- **语音播报提醒** — 提醒触发时语音播报事件信息
- **WebSocket 推送** — 服务端定时检查并推送即将到期的提醒

> **技术说明：** 提醒检查为 30 秒轮询机制（可配置），非实时推送。最坏情况下提醒延迟约 30 秒。提醒仅在浏览器打开且 WebSocket 连接正常时送达。

### 体验设计

- **深色模式** — 暗色主题为默认，支持 light/dark 切换
- **多语言界面** — 界面文案跟随语言切换（中/英/日）
- **零门槛** — Web 应用，打开浏览器即用，无需注册登录
- **环境光效** — 浮动渐变光球动画，玻璃态面板
- **响应式** — 适配桌面和移动端布局

---

## 竞品对比

| 维度 | Voice Calendar | 0sec | Fantastical | Siri / 小爱 | Motion |
|------|---------------|------|-------------|------------|--------|
| 中文支持 | 原生支持 | ❌ | ❌ | 基础指令 | ❌ |
| 语音核心度 | 高（语音为唯一输入方式） | 高 | 中（文本为主） | 高（系统级） | 低（键盘为主） |
| 复杂指令理解 | 中（4 类意图 + 歧义处理） | 中 | 强（多年迭代） | 弼（简单模板） | 中 |
| 语音播报 | 有 | 有 | 无 | 有 | 无 |
| 开源可自部署 | 是 | 否 | 否 | 否 | 否 |
| 价格 | 免费（AI API 按量付费） | 免费 | $4.99/月 | 免费 | $19/月 |
| 平台 | Web（跨平台） | iOS | Apple 全系 | 各自生态 | 全平台 |

### 实际差异化

1. **中文自然语言解析** — 国内唯一专注语音+AI 的日历工具
2. **语音播报闭环** — 不仅识别，还用语音反馈确认结果
3. **Web 零门槛** — 无需安装 App，打开浏览器即用
4. **开源可自部署** — 数据完全掌控，AI 提供商可自由切换

---

## 技术架构

```
┌──────────────────────────────────────────────────────┐
│                    浏览器 (前端)                        │
│                                                        │
│  ┌──────────┐  ┌──────────┐  ┌────────────────────┐   │
│  │ Web Speech│  │ Calendar │  │ Event Panel        │   │
│  │ 语音识别   │  │ 日历视图  │  │ 解析卡片 + 事件列表 │   │
│  └─────┬────┘  └──────────┘  └────────┬───────────┘   │
│        │ 语音文本                    确认/取消          │
│        ▼                              │               │
│  ┌──────────────────────────────────┐ │               │
│  │          api.js (Fetch)           │ │               │
│  └──────────────┬───────────────────┘ │               │
└─────────────────┼─────────────────────┼───────────────┘
                  │ HTTP/WS             │
                  ▼                     ▼
┌───────────────────────────────────────────────────────┐
│                 Node.js + Express 后端                   │
│                                                         │
│  ┌──────────────┐  ┌──────────────┐  ┌─────────────┐   │
│  │ /api/voice    │  │ /api/events  │  │ WebSocket   │   │
│  │ 语音指令解析   │  │ 事件 CRUD    │  │ 提醒推送     │   │
│  └──────┬───────┘  └──────┬───────┘  └─────────────┘   │
│         │                 │                             │
│  ┌──────▼────────┐  ┌─────▼────────┐                    │
│  │ AI Parser     │  │ Event DAO    │                    │
│  │ LLM 调用+Prompt│  │ SQLite CRUD  │                    │
│  └──────┬────────┘  └──────────────┘                    │
│         │                                               │
│  ┌──────▼──────────────────────────────────┐            │
│  │ DeepSeek / OpenAI / Qwen (兼容格式 API) │            │
│  └─────────────────────────────────────────┘            │
└─────────────────────────────────────────────────────────┘
```

### 技术栈

| 层级 | 技术 | 说明 |
|------|------|------|
| 前端 | HTML / CSS / JavaScript | 原生实现，无框架依赖 |
| 语音识别 | Web Speech API | 浏览器原生，支持中/英/日 |
| 语音播报 | SpeechSynthesis API | 浏览器原生语音合成 |
| 后端 | Node.js + Express | RESTful API + WebSocket |
| 数据库 | SQLite (sql.js) | 纯 WASM 实现，单文件存储 |
| AI 解析 | OpenAI 兼容格式 API | 可切换 DeepSeek / Qwen / OpenAI |
| 实时通信 | WebSocket (ws) | 提醒推送（30 秒轮询检查） |

### 已知技术限制

| 限制 | 原因 | 影响 |
|------|------|------|
| 语音识别需 Chrome/Edge | Web Speech API 仅 Chromium 内核完整支持 | Safari 部分可用，Firefox 不可用 |
| 不支持方言 | Web Speech API 仅支持标准语言代码 | 仅普通话/标准英语/标准日语 |
| 不支持农历 | 无农历转换库，LLM 对农历日期不可靠 | 农历相关指令无法准确解析 |
| 提醒有 30 秒延迟 | 采用轮询检查机制 | 精确到分钟级别，非秒级 |
| 单用户架构 | SQLite 单文件 + 内存会话 | 不支持多用户并发 |
| 需 HTTPS 部署 | Web Speech API 安全上下文要求 | 本地开发可用 localhost，部署需 HTTPS |
| AI 解析有 1-3 秒延迟 | LLM 推理耗时 | 语音→操作完成总耗时约 3-5 秒 |
| 重复事件存储但未展开 | LLM 可生成 recurrence_rule 但前端未渲染实例 | 创建了重复规则但日历只显示一条 |

---

## 快速开始

### 环境要求

- Node.js >= 18
- Chrome 或 Edge 浏览器（语音识别必需）
- 麦克风设备
- 可用的 AI API Key（DeepSeek / OpenAI / Qwen）

### 安装

```bash
git clone <repo-url> voice-calendar
cd voice-calendar
npm install
```

### 配置

```bash
cp .env.example .env
```

编辑 `.env` 文件填入你的 AI API 配置：

```env
AI_API_KEY=your-api-key-here
AI_API_BASE=https://api.deepseek.com/v1
AI_MODEL=deepseek-v4-pro
```

支持的 AI 提供商：

| 提供商 | AI_API_BASE | 测试过的模型 |
|--------|------------|-------------|
| DeepSeek | `https://api.deepseek.com/v1` | deepseek-v4-pro |
| OpenAI | `https://api.openai.com/v1` | gpt-4o-mini |
| 通义千问 | `https://dashscope.aliyuncs.com/compatible-mode/v1` | qwen-turbo |

### 启动

```bash
node server.js
```

打开浏览器访问 `http://localhost:3000`

> **部署注意：** 部署到服务器时必须启用 HTTPS，否则浏览器会拒绝麦克风权限和语音识别。

### 使用语音

1. 点击右侧面板的麦克风按钮
2. 说出指令
3. 查看解析结果卡片，确认或取消
4. 操作完成后会听到语音播报

---

## 支持的语音指令

### 创建事件

- "明天下午两点开会"
- "后天上午十点产品评审会议"
- "每周一到周五早上九点站会"
- "这周每天晚上八点提醒我复习"
- "Schedule a meeting tomorrow at 3pm"

### 查询事件

- "今天有什么安排"
- "这周有什么事"

### 删除事件

- "删掉明天的会议"
- "取消后天的约会"

### 修改事件

- "把下午的会改到四点"
- "把明天的会议改到后天"

> **注意：** 当指令有歧义时（如"删掉明天的会"但明天有多个事件），系统会列出候选让你选择。如果解析结果不准确，可以取消后重新说。

---

## 项目结构

```
voice-calendar/
├── server.js                       # Express 入口 + WebSocket 服务
├── .env.example                    # 环境变量模板
├── package.json
├── src/
│   ├── ai/
│   │   └── parser.js               # LLM 调用 + Prompt 管理
│   ├── db/
│   │   ├── init.js                 # 数据库初始化（建表）
│   │   ├── event-dao.js            # 事件增删改查
│   │   └── reminder-dao.js         # 提醒记录管理
│   ├── routes/
│   │   ├── events.js               # /api/events RESTful API
│   │   └── voice.js                # /api/voice 语音指令解析
│   ├── services/
│   │   └── reminder.js             # 提醒检查 + WebSocket 推送
│   └── public/
│       ├── index.html              # 单页应用主页面
│       ├── css/                    # 样式文件（4 个）
│       └── js/
│           ├── app.js              # 应用入口 + 事件绑定
│           ├── voice.js            # 语音识别状态机
│           ├── calendar-ui.js      # 月/周/日视图渲染
│           ├── event-panel.js      # 解析卡片 + 事件列表
│           ├── api.js              # 后端 API 封装
│           ├── tts.js              # 语音播报
│           ├── i18n.js             # 国际化引擎
│           └── i18n/
│               ├── zh-CN.js
│               ├── en-US.js
│               └── ja-JP.js
└── docs/
    └── PRODUCT_SPEC.md
```

---

## API 接口

### 事件 CRUD

| 方法 | 路径 | 说明 |
|------|------|------|
| `GET` | `/api/events?start=&end=` | 获取日期范围内的事件 |
| `GET` | `/api/events/:id` | 获取单个事件 |
| `POST` | `/api/events` | 创建事件 |
| `PUT` | `/api/events/:id` | 更新事件 |
| `DELETE` | `/api/events/:id` | 删除事件 |

### 语音指令

| 方法 | 路径 | 说明 |
|------|------|------|
| `POST` | `/api/voice/parse` | 解析语音文字 + 查询/歧义处理 |
| `POST` | `/api/voice/execute` | 执行已确认的操作（add/delete/update） |

**请求示例：**

```json
POST /api/voice/parse
{
  "text": "明天下午两点开会",
  "language": "zh-CN",
  "context": { "today": "2026-05-31T00:00:00Z" }
}
```

**响应示例：**

```json
{
  "success": true,
  "intent": "add_event",
  "params": {
    "title": "开会",
    "start_time": "2026-06-01T14:00:00",
    "end_time": "2026-06-01T15:00:00",
    "reminder_minutes": 15
  },
  "reply": "已为您安排明天下午2点的会议，是否确认？",
  "needs_confirm": true
}
```

---

## 配置项

| 变量 | 默认值 | 说明 |
|------|--------|------|
| `PORT` | `3000` | 服务端口 |
| `AI_API_KEY` | - | LLM API 密钥（必填） |
| `AI_API_BASE` | `https://api.openai.com/v1` | API 基础地址 |
| `AI_MODEL` | `gpt-4o-mini` | 模型名称 |
| `DB_PATH` | `./data/calendar.db` | 数据库文件路径 |
| `REMINDER_INTERVAL` | `30` | 提醒检查间隔（秒） |

---

## 浏览器兼容性

| 浏览器 | 语音识别 | 语音播报 | 通知 | 推荐度 |
|--------|---------|---------|------|--------|
| Chrome 80+ | ✅ | ✅ | ✅ | 推荐 |
| Edge 80+ | ✅ | ✅ | ✅ | 推荐 |
| Safari 14+ | ⚠️ 部分支持 | ✅ | ✅ | 可用 |
| Firefox | ❌ | ✅ | ✅ | 不推荐 |

---

## 已知问题

| 问题 | 状态 | 说明 |
|------|------|------|
| 设置页面仅保存到浏览器 | 已知 | 修改 API Key 需直接编辑服务器 .env 文件，前端设置页暂不生效 |
| 解析卡片字段不可编辑 | 已知 | 解析不准确时需取消后重新说，暂不支持在卡片上直接修改 |
| 重复事件仅存储不展开 | 已知 | 创建"每周一开会"后日历只显示一条记录，不会生成每日实例 |
| 周视图切回月视图可能布局错乱 | Bug | CSS class 未重置，刷新页面可恢复 |
| 事件加载范围固定按月 | Bug | 周视图跨月时可能遗漏事件 |

---

## Roadmap

### v1.0 — 语音日历助手（当前）

- [x] 语音识别 + AI 自然语言解析
- [x] 事件 CRUD 全流程语音操控
- [x] 月/周/日日历视图
- [x] 轮询提醒 + 浏览器通知 + 语音播报
- [x] 深色模式 + 多语言（中/英/日）
- [x] 删除事件歧义处理（候选列表）
- [ ] 解析卡片可编辑（确认前修改字段）
- [ ] 设置页面联动后端
- [ ] 修复已知 Bug（CSS class、事件加载范围）

### v1.1 — 体验完善

- [ ] 语音按钮交互优化（长按/短按统一）
- [ ] 语音错误反馈（权限拒绝、网络错误提示）
- [ ] 日历周起始日本地化（中文/日文周一起始）
- [ ] 日历点击联动侧边栏（点击日期显示当天事件）
- [ ] API 错误处理 + 超时机制
- [ ] 输入校验 + 频率限制
- [ ] 提醒音效

### v2.0 — 时间管理 Agent

- [ ] 多轮对话 + 上下文记忆
- [ ] 日程冲突检测 + 替代时间段建议
- [ ] 智能提醒默认值（会议 15 分钟、就医 60 分钟）
- [ ] 用户偏好学习

### v3.0 — 个人效率 Agent（远期探索）

- [ ] 主动日程建议
- [ ] 周/月报自动生成
- [ ] 与第三方日历同步（Google Calendar / Apple Calendar）

---

## 竞品参考

| 产品 | 链接 | 说明 |
|------|------|------|
| 0sec | [App Store](https://apps.apple.com/us/app/0sec-voice-ai-calendar/id6752616667) | 纯语音优先日历（仅 iOS，仅英文） |
| Fantastical | [flexibits.com](https://flexibits.com/fantastical) | 行业标杆级自然语言日历（仅英文） |
| Motion | [usemotion.com](https://www.usemotion.com/) | AI 自动排程 |
| Morgen | [morgen.so](https://www.morgen.so/) | AI 每日规划 |
| Any.do | [any.do](https://www.any.do/) | 任务 + 日历 + 语音集成 |
| Caliq | [App Store](https://apps.apple.com/tw/app/natural-calendar-caliq/id6757199286) | 自由文本转日历事件 |

---

## License

MIT
